# 2020-01-23

​	经过两三天的摸索，要写出一个简单能用的数据库挺难的，所以一开始要定位清楚自己到底要实现到什么层次。虽然我很想实现后端，也就是B-tree、Pager、OS Interface。但是考虑到时间、精力、能力等，暂时只实现简单的增删改查吧。

​	Holo_DB运行的形式，暂时就只能以.exe的方式运行，不像是sqlite3那样，提供.h文件，然后能够嵌入程序中。

​	今天目标是封装一个文件操作类和日志模块

# 2020-01-24

**1、使用snprintf函数**
// 将(...)可变参数按照format格式化成字符串，并将字符串复制到str中
// snprintf(char* str, size_t size, const char* format, ...);
// str: 目标字符串
// size: 要写入的字符的最大数目
// format: 格式化成字符串
// 返回值：
// 1,如果格式化后的字符串长度<=size,则会把字符串全部复制到str中,并给其后添加一个字符串结束符\0
// 2,如果格式化后的字符串长度>size,则会被截断

**2、使用vsnprintf函数**：

​	需要包含头文件:#include<stdio.h>、<stdarg.h>

​	函数原型：int vsnprintf(char* str, size_t size, const char* format, va_list ap);

​	函数说明：将可变参数格式化输出到一个**字符数组**

```
#include <cstdarg>
#include <cstdio>

int test_fun(const char* fmt, ...) {
  char str_tmp[100];
  va_list ap;
  va_start(ap, fmt);
  vsnprintf(str_tmp, 100, fmt, ap);
  va_end(ap);
  printf("%s", str_tmp);
  return 1;
}

int main() {
  test_fun("%s,%d,%s\n", "aaa", 123, "bbb");
  return 0;
}

//test_fun传入什么参数,fmt就是什么参数
```

# 2020-01-25

​	今天查阅了数据库相关的资料，然后买了一本书《数据库系统的实现》，看内容介绍还挺详细的，设计到了很底层的知识。

​	昨天的日志模块暂时能用了，目前只是实现了在终端打印，写入文件的操作后续再说。

```c++
#define hldb_log_debug(fmt, ...)                                          \
  HoloDBLog::GetInstance()->logger_print(                                 \
      LOG_LEVEL_DEBUG, "[%s][%d行][%s]" fmt "\n", __FILENAME__, __LINE__, \
      __FUNCTION__, ##__VA_ARGS__)
```

​	很关键一点就是fmt对应的内容就是##\__VA_ARGS__,其实...可以写成arg...，这样##\__VA_ARGS__就可以换成##arg了。

​	关于文件操作的封装，今天看了挺多的，但是没有看到我想要的实现，自己试着封装了一个，但是感觉没什么用...。对于文件操作，其实完全可以走C，使用C库。

​	日志模块需要对文件进行读写操作，后续数据库肯定也要频繁的对文件进行读写操作(系统IO，对磁盘进行读写)，所以关于文件操作这一块还是需要研究，今天打算再看看sqlite3的实现，希望能有所收获。

